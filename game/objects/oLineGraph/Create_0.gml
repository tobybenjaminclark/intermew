/// @description Initialize Variables

// Data arrays
x_values = [0, 1, 2, 3, 4, 5];
y_values = [0, 1, 2, 3, 4, 5];

// Graph settings
ox = x; // Starting X position of the graph
oy = y; // Starting Y position of the graph
w = 200; // Width of the graph
h = 200; // Height of the graph

mx = max_in_arr(x_values);
my = max_in_arr(y_values);
